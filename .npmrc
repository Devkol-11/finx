shamefully-hoist=true

; Install native binaries for both Windows and Linux so the repo works
; from Windows PowerShell and WSL without reinstalling each time.
supportedArchitectures[os][]=win32
supportedArchitectures[os][]=linux
supportedArchitectures[cpu][]=x64
